// fastmm-gateway: the venue connections strategy processes attach to (fastmm::cli::gateway,
// src/live/gateway_cli.cpp).
#include "fastmm/cli/gateway.hpp"

int main(int argc, char** argv) {
  return fastmm::cli::gateway(argc, argv);
}
