#pragma once
// fastmm::cli::gateway: the fastmm-gateway command line (live/gateway.hpp) as a library function.
// Run `--help` for the flags and exit codes.

namespace fastmm::cli {

int gateway(int argc, char** argv);

}  // namespace fastmm::cli
