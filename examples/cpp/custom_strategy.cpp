// Placeholder: replaced by a real custom-strategy example once the Strategy API lands.
#include "fastmm/version.hpp"

#include <cstdio>

int main() {
  std::printf("custom_strategy example - %s\n", fastmm::build_info());
  return 0;
}
