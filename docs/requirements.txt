# Python packages that build the documentation site (mkdocs.yml). Python 3.10 or later.
#
#   ./scripts/docs-serve.sh          local preview, in build/docs-venv
#   .github/workflows/docs.yml       the same pins in CI
#
# The C++ reference also needs Doxygen 1.9.8 or later, which is not a Python package
# (`apt install doxygen`).
#
# Every version is pinned, transitive dependencies included: the site must build the same way
# in a year. To move to a newer Material release, install the four direct dependencies below
# without pins in a fresh virtual environment and paste `pip list --format=freeze` back here.

# direct
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocstrings==1.0.6
mkdocstrings-python==2.0.9
ruff==0.15.4  # mkdocstrings uses it to format Python signatures

# transitive
Jinja2==3.1.6
Markdown==3.10.3
MarkupSafe==3.0.3
PyYAML==6.0.3
Pygments==2.21.0
babel==2.18.0
backrefs==8.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
ghp-import==2.1.0
griffelib==2.3.0
idna==3.20
mergedeep==1.3.4
mkdocs-autorefs==1.4.4
mkdocs-get-deps==0.2.2
mkdocs-material-extensions==1.3.1
packaging==26.3
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.11.12
properdocs==1.6.7
pymdown-extensions==12.1
python-dateutil==2.9.0.post0
pyyaml_env_tag==1.1
requests==2.34.2
six==1.17.0
urllib3==2.8.0
watchdog==6.0.0
