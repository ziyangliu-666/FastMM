// Placeholder translation unit so the fastmm_venues library target has at least one object
// until the first real source lands. Safe to delete afterwards.
namespace fastmm::detail {
int fastmm_venues_lib_anchor() noexcept {
  return 0;
}
}  // namespace fastmm::detail
